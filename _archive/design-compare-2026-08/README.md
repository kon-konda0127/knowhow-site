# デザイン比較検討ページ（アーカイブ・2026-08-14移動）

## これは何か
2026年8月のノウハウサイト全面リデザインにあたり、デザイン案を比較検討するために作った資料です。

- `design-compare.html` … 27パターンを一覧して見比べるための入口ページ
- `pattern-a/` 〜 `pattern-z/` `pattern-aa/` … 各パターンの実物（全27フォルダ）

## なぜ公開フォルダの外へ移したか
Manus案という決定版が採用され、比較検討ページを公開し続ける理由がなくなったためです（仕様書 `docs/spec_knowhow_redesign.md` 15章1の決定に沿った作業）。

このリポジトリはGitHub Pagesで **`docs/` フォルダの中身だけ** が公開される設定です。ここ（`_archive/`）は `docs/` の外なので、**ファイルは残っていますが本番サイトからは開けません**。

## 元に戻したい時
削除ではなく移動なので、いつでも戻せます。ターミナルで以下を実行してください。

```bash
cd ~/Documents/claude/apps/knowhow-site
git mv _archive/design-compare-2026-08/design-compare.html docs/
for d in _archive/design-compare-2026-08/pattern-*; do git mv "$d" docs/; done
```

## 補足
- 各パターン内のページは相対パスで自分のフォルダ内を参照しているため、フォルダごと動かしても中身の表示は壊れません
- 本番サイトの他のページからこれらへのリンクは元々ありません（移動によるリンク切れは発生しません）
- `docs/design-specs.md`（デザイン仕様のメモ）にはパターンへの参照が残っています。こちらはサイトのページからリンクされていない作業メモのため、そのまま置いてあります
